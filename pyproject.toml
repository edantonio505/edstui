[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "eds-tui"
version = "1.0.0"
description = "Ed's terminal AI assistant your LLM"
requires-python = ">=3.9"
dependencies = [
    "ollama",
    "rich",
]

[project.scripts]
ask = "eds_tui.main:main"
